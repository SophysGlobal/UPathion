// Premium AI Chat moved into the BottomNav. This component is intentionally
// a no-op to preserve existing imports across pages without rendering the
// floating action button anymore.
const PremiumChatFAB = () => null;

export default PremiumChatFAB;
